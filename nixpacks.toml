[phases.setup]
nixPkgs = ["python311", "gcc", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python -m app.db.init_db"]

[start]
cmd = "uvicorn app.api.server:app --host 0.0.0.0 --port $PORT"
